# קולורטורה

פרויקט שממיר ציור למוזיקה — לא באמצעות טבלת המרה שרירותית ("צהוב = דו"), אלא דרך מנוע שמחלץ תכונות חזותיות אמיתיות (צבע, קו, קומפוזיציה), מתרגם אותן דרך שכבת תיווך רגשית מבוססת-מחקר, ומרכיב מהן תקציר מוזיקלי שמוזן למנוע הרמוניה פרוצדורלי עם ניהול-קולות אמיתי בארבעה קולות (SATB).

השם עצמו כבר גשר בין שני התחומים: "קולורטורה", במקור הלטיני, פירושו "צביעה" — ובעולם הזמרה האופראית זה הכינוי לקישוט מלודי מפואר.

## הצינור

```
ציור (JPG)
  │
  ├─ feature_extraction.py    חילוץ תכונות CV: בהירות, רוויה, טמפרטורת צבע, עובי/עקמומיות/
  │                           צפיפות קו, סימטריה, שטח שלילי, מוקד ניגודיות, כיוון תנועה
  │
  ├─ semantic_features.py     ציוני CLIP (zero-shot) לערכיות/עוררות/מתח וסיווג סגנון —
  │                           השכבה שתופסת תוכן רגשי שסטטיסטיקת פיקסלים עיוורת אליו
  │                           (למשל שמיים בוערים מול פיורד שחור אצל מונק)
  │
  ├─ mapping_engine.py        ממזג את שתי השכבות למרחב תיווך: ערכיות, עוררות, מתח-פורמלי
  │                           (ניגוד צבע/גיאומטריה) ומתח-רגשי (איום/רזולוציה) — שני צירי
  │                           מתח נפרדים, לא אחד, אחרי שהתברר שהם עונים על שאלות שונות.
  │                           הפלט: "תקציר מוזיקלי" — אובייקט JSON עם סולם, מודוס, טמפו,
  │                           מטר, צורה, תזמור, ארטיקולציה, עקומת דינמיקה ורזולוציה הרמונית
  │
  ├─ composer.py               גרסת "lite": התקציר → MIDI עם פרוגרסיה קבועה (בסיס להשוואה)
  │
  ├─ stage_a.py                הגרסה המלאה: דקדוק הרמוני פונקציוני (Markov משוקלל מעל
  │                            טוניקה/סאב-דומיננטה/דומיננטה) + ניהול-קולות אילוצי בארבעה
  │                            קולות — תנועה מינימלית, בלי מקבילים של חמישית/אוקטבה, רזולוציית
  │                            קדנצה מודעת-מודוס, מטר אמיתי (7/8 = 2+2+3), צורת ABA/דרך-מולחן
  │
  ├─ synth.py                  MIDI → WAV: סינתיסייזר אדיטיבי עצמאי (בלי SoundFont/בינארי
  │                            חיצוני) עם פרופיל הרמוניות ומעטפת ADSR לכל משפחת כלי-GM
  │
  ├─ stage_b.py                 (אופציונלי, דורש מפתח API) שולח את ה-WAV של דרגה A
  │                            ל-Suno (דרך sunoapi.org) כ"cover" reference + prompt
  │                            טקסטואלי שנבנה מהתקציר, ומקבל בחזרה רינדור AI ריאליסטי
  │
  └─ webapp.py + static/         MVP אתר (FastAPI) — דרגה A בלבד, ראו "אתר" למטה
```

## הרצה

```bash
pip install -r requirements.txt
python src/run_test.py      # מריץ על 5 ציורי הבדיקה ב-test_images/, כותב ל-output/
python src/render_wav.py    # ממיר את כל קובצי ה-MIDI שנוצרו ל-WAV
```

### אתר (MVP, דרגה A בלבד)

```bash
uvicorn webapp:app --app-dir src --port 8000
```

מוגן ב-HTTP Basic Auth (כל route, כולל `/api/*`) — CLIP/CV לא בחינם מבחינת CPU גם אם אין עלות כספית ישירה. אם `WEBAPP_USER`/`WEBAPP_PASSWORD` לא מוגדרים ב-`.env`, מודפסת סיסמה אקראית לקונסולה בכל הפעלה מחדש של השרת (בלי ברירת מחדל קבועה בקוד). להגדרת סיסמה קבועה: מוסיפים ל-`.env`.

פותחים `http://127.0.0.1:8000` (הדפדפן יבקש שם משתמש/סיסמה) — גוררים או בוחרים ציור, ותוך כמה שניות מקבלים תקציר מלא (VAT, סולם, תזמור, סיווג סגנון) ונגן שמע עם רינדור דרגה A + כפתור הורדה. דרגה B נשארת שלב נפרד ומכוון-ידנית (`run_stage_b.py`) כי היא עולה כסף אמיתי לכל קריאה.

נבדק בפועל מקצה-לקצה, כולל ניגון שמע אמיתי, בדפדפן רגיל — כל השדות בתקציר, מטרי ה-VAT, וסיווג הסגנון מוצגים נכון ותואמים בדיוק לפלט ה-CLI. (תוך כדי הבדיקה נתקלתי בניגון שנתקע *בתוך סביבת האוטומציה* של Claude Code — `readyState` של ה-`<audio>` לא התקדם אפילו עבור קובץ MP3 חיצוני ידוע-כתקין; אושר שזו מגבלת הסביבה האוטומטית ולא באג באפליקציה, לאחר שהניגון עבד תקין בבדיקה ידנית בדפדפן רגיל.)

#### פריסה לענן (Hugging Face Spaces)

נבחר על פני Render/Railway/Fly.io: ה-tier החינמי של Spaces נותן 16GB RAM בלי כרטיס אשראי, מספיק בנוחות ל-torch+CLIP (ל-Render/Fly יש רק 512MB-1GB בחינם — צפוי להיתקע). `Dockerfile` בשורש הריפו בנוי בהתאם (torch מותקן מפורשות מ-index של CPU-בלבד, כדי לא למשוך wheels של CUDA מיותרים בגודל כמה GB).

```bash
python deploy/push_to_space.py
```

דורש (ב-`.env`): `HF_USERNAME`, `HF_TOKEN` (טוקן עם הרשאת write, מ-[huggingface.co/settings/tokens](https://huggingface.co/settings/tokens)), ו-Space בשם `coloratura` שכבר נוצר תחת החשבון (SDK: Docker). הסקריפט בונה עותק נקי (Dockerfile + requirements.txt + src/ + README ייעודי עם ה-YAML frontmatter שדורש HF) בתיקייה זמנית, ודוחף אותו כהיסטוריית git נפרדת לגמרי מהריפו הזה — כי ל-README של ה-Space יש דרישות פורמט (frontmatter) שלא רלוונטיות ל-README הזה.

**לא נבדק בפועל** — Docker לא זמין בסביבה שבה נכתב הקוד הזה, אז ה-build הראשון ב-Spaces הוא המבחן האמיתי.

### דרגה B (אופציונלי, בתשלום)

דורש מפתח API מ-[sunoapi.org](https://sunoapi.org). חשוב: ל-Suno אין מנגנון "בצע את התווים האלה בדיוק" — ה-`cover` endpoint מקבל **URL** לקובץ אודיו קיים (לא MIDI) כהשראה, ומייצר סאונד חדש בהתאם לפרומפט טקסטואלי שנבנה מהתקציר. זו פרשנות מחדש של ה-AI, לא ביצוע נאמן של ניהול-הקולות מדרגה A.

```bash
export SUNO_API_KEY=sk-...          # (Windows: $env:SUNO_API_KEY="sk-...")
python src/run_stage_b.py           # כל 5 הציורים — עולה כסף, כ-2-3 דקות לציור
python src/run_stage_b.py munch_the_scream   # ציור בודד
```

הקובץ המקורי (רינדור דרגה A) שמשמש כ-reference חייב להיות בכתובת ציבורית — הוא כבר מתארח ב-`assets/stage_a_wav/` בריפו הזה, ונקרא דרך raw.githubusercontent.com.

`output/` (לא נשמר בגיט, נוצר מחדש בכל הרצה):
- `*.json` — התקציר המלא לכל ציור
- `report.txt` — טבלת השוואה + פירוט מלא לכל ציור
- `midi/lite/`, `midi/stage_a/` — קובצי MIDI
- `wav/lite/`, `wav/stage_a/` — קובצי WAV

## ציורי הבדיקה

חמישה ציורים ציבוריים (public domain, Wikimedia Commons) שנבחרו קיצוניים מבחינה סגנונית: ואן גוך (ליל כוכבים), קנדינסקי (קומפוזיציה 8), מונדריאן (קומפוזיציה II), מונה (נימפיאות), מונק (הצעקה) — כדי לבחון האם המיפוי מבחין ביניהם בצורה שמרגישה נכונה למי שמכיר את שתי השפות.

## דוגמאות שמע

כל 5 הציורים, בשני שלבי איכות — מתארח ב-`assets/` בריפו הזה, נגיש ישירות דרך raw.githubusercontent.com:

| ציור | דרגה A (MIDI→WAV עצמאי) | דרגה B (Suno) |
|---|---|---|
| ואן גוך — ליל כוכבים | [WAV](assets/stage_a_wav/van_gogh_starry_night.wav) | [MP3](assets/stage_b_mp3/van_gogh_starry_night.mp3) |
| קנדינסקי — קומפוזיציה 8 | [WAV](assets/stage_a_wav/kandinsky_composition_8.wav) | [MP3](assets/stage_b_mp3/kandinsky_composition_8.mp3) |
| מונדריאן — קומפוזיציה II (קדנצה **פתורה**) | [WAV](assets/stage_a_wav/mondrian_composition_ii.wav) | [MP3](assets/stage_b_mp3/mondrian_composition_ii.mp3) |
| מונה — נימפיאות | [WAV](assets/stage_a_wav/monet_water_lilies.wav) | [MP3](assets/stage_b_mp3/monet_water_lilies.mp3) |
| מונק — הצעקה (קדנצה **לא-פתורה**) | [WAV](assets/stage_a_wav/munch_the_scream.wav) | [MP3](assets/stage_b_mp3/munch_the_scream.mp3) |
| הוקוסאי — הגל הגדול מול קנגאווה `*` | [WAV](assets/stage_a_wav/hokusai_great_wave.wav) | [MP3](assets/stage_b_mp3/hokusai_great_wave.mp3) |
| קלימט — הנשיקה `*` | [WAV](assets/stage_a_wav/klimt_the_kiss.wav) | [MP3](assets/stage_b_mp3/klimt_the_kiss.mp3) |
| קרוואג'ו — יהודית עורפת את ראש הולופרנס `*` (קדנצה **לא-פתורה**) | [WAV](assets/stage_a_wav/caravaggio_judith.wav) | [MP3](assets/stage_b_mp3/caravaggio_judith.mp3) |

`*` לא חלק מחמשת ציורי הבדיקה המקוריים — נוספו אחר-כך כבדיקת הכללה על סגנונות שלא אומן עליהם. הוקוסאי (אוקיו-אה יפני) התפצל 55% ריאליזם / 38% סוריאליזם; קלימט (ארט-נובו/סימבוליזם) התפצל 36% קוביזם-גאומטרי / 27% מינימליזם / 17% אקספרסיוניזם — אף אחת מ-7 קטגוריות הסגנון (פרק ז במסמך היסוד) לא מתאימה במדויק לאף אחד מהם. סימן טוב, לא כשל: המנוע לא מתעקש על ביטחון שווא כשהוא נתקל במשהו זר. קלימט קיבל את מתח-הרגשי (`tension_emotional`) הנמוך ביותר מכל שמונת הציורים — 0.19 — תואם לתוכן האינטימי. קרוואג'ו, לעומתו, קיבל את השני-בגובהו — 0.66 (אחרי מונק, 0.85) — וגם קדנצה לא-פתורה, מתאים לרגע האלים שבתמונה. הפעם דווקא סיווג הסגנון היה חד — 80% ריאליזם, הביטחון הגבוה ביותר עד כה — כי הכיאורוסקורו הדרמטי-פוטוגרפי של קרוואג'ו מתאים היטב לקטגוריה.

מונדריאן ומונק הם ההשוואה הכי מומלצת — אותה שלד הרמוני בסיסי, אבל מונק היחיד שמסתיים בלי רזולוציה (ר' "ממצא מרכזי" למטה). שני אלה גם מוטמעים ישירות (base64, לא קישור) במסמך היסוד עצמו.

## ממצא מרכזי מהבדיקה

הרצה ראשונית עם VAT (ערכיות/עוררות/מתח) חד-ממדי נתנה תוצאה הפוכה: מונדריאן קיבל ציון "מתח" גבוה יותר ממונק. הסיבה — מדד מבוסס-פיקסלים קרא ניגוד צבעים, לא איום. הרשת של מונדריאן צורמת לעין אך רגועה לגמרי מבחינה רגשית; אצל מונק זה הפוך. זה מה שהוביל לפיצול "מתח" לשני צירים נפרדים (`tension_formal` ו-`tension_emotional`) שמזינים יעדים הרמוניים שונים.

## מה עדיין חסר / מפושט

- שכבת היצירה מרנדרת רק כיוון אחד (ציור ← מוזיקה). הכיוון ההפוך (מוזיקה ← ציור) לא הותחל.
- הבס תמיד בפוזיציית-יסוד (בלי היפוכים); רק קול הסופרן מקבל תווי-מעבר/שכנים מלודיים.
- הסינתיסייזר העצמי (`synth.py`) פשוט (אדיטיבי, לא SoundFont) — לא נשמע כמו תזמורת אמיתית.
- `stage_b.py` עובד קצה-לקצה מול ה-API החי (נבדק בפועל על "הצעקה" של מונק — ג'נרציה מלאה, MP3 תקין הורד בהצלחה). זו עדיין פרשנות-מחדש של Suno על בסיס reference audio + prompt, לא ביצוע נאמן של ניהול-הקולות מדרגה A — ראו הערה בראש `stage_b.py`.
- סיווג הסגנון וקטגוריות הפונקציה ההרמונית (T/S/D) הם הקונבנציה של מוזיקה מערבית קלאסית; לא הותאמו למסורות מוזיקליות אחרות.
