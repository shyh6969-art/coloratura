SUNO_API_KEY=

# image_stage_b.py — AI-quality image rendering (OpenAI gpt-image-2).
# Create a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# webapp.py (Stage A web MVP) HTTP Basic Auth — optional. Leave blank and
# a random password is generated and printed on each server startup instead.
WEBAPP_USER=
WEBAPP_PASSWORD=

# deploy/push_to_space.sh — pushing the web MVP to a Hugging Face Space.
# Create a token at https://huggingface.co/settings/tokens (role: write).
HF_USERNAME=
HF_TOKEN=
